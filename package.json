{
  "name": "esgi--formation-api",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix server"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "prettier": "^2.6.1"
  }
}
